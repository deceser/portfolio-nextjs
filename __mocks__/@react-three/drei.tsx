import React from 'react';

export const CameraControls = () => null;
export const useTexture = jest.fn(() => ({}));
export const Sky = () => null;
export const MeshPortalMaterial = ({ children }: { children: React.ReactNode }) => <>{children}</>;
